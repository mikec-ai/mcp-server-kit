name: "{{nameWithSpaces}} - Basic"
description: "{{#if description}}{{description}}{{else}}Verify that {{name}} tool works correctly{{/if}}"
tool: "{{name}}"
arguments:
  # TODO: Add test arguments
  # Example:
  # message: "test message"

assertions:
  - type: "success"
  - type: "response_time_ms"
    max: 5000
  # TODO: Add more assertions
  # - type: "contains_text"
  #   text: "expected output"
  # - type: "json_path"
  #   path: "$.result"
  #   expected: "expected value"
